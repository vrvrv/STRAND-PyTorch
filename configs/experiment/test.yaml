# @package _global_

data_name: ts

model:
  e_iter: 1
  nmf_max_iter: 100
  tf_max_steps: 1
  laplace_approx_conf:
    lr: 0.01
    max_iter: 10
    batch_size: 512

trainer:
  max_epochs: 1

logger:
  _target_: pytorch_lightning.loggers.wandb.WandbLogger
  project: "STRAND-Lightning-Simulation1"
  save_dir: "."
  group: ${data_name}
  name: test